# Day 34 - NumPy & Rasterio

## 📚 What I Learned

Today I learned:

- NumPy arrays for raster data
- 2D arrays and raster pixels
- `.shape` for checking array dimensions
- NumPy calculations such as `mean()`, `max()`, and `min()`
- Boolean filtering in NumPy
- `np.where()`
- Basic raster processing with Rasterio
- Reading raster data
- Raster bands
- Raster CRS and transform
- Writing GeoTIFF files
- Basic DEM analysis

## 🌍 GIS Application

Using NumPy and Rasterio to work with raster data such as:

- DEM
- Elevation data
- Satellite imagery
- Environmental raster data
