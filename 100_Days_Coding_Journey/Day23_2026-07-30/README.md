# Day 23 - Module & Import

## 📚 What I Learned

Today I learned:

- What a **module** is
- How to use `import`
- `math.sqrt()`
- `math.pow()`
- `math.ceil()` and `math.floor()`
- `math.pi`
- `from ... import ...`
- `random.randint()`
- Creating reusable functions

---

## 💻 Practice

✔ Calculate square roots with `math.sqrt()`

✔ Calculate powers with `math.pow()`

✔ Round numbers using `ceil()` and `floor()`

✔ Calculate the area of a circle with `math.pi`

✔ Generate random numbers with `randint()`

✔ Create a `distance()` function

---

## 🌍 Mini GIS

Built a **Nearest Station Finder**.

Features:

- Store station coordinates
- Calculate distance from a center point
- Find the **nearest station**
- Print a simple GIS report

---

## 🏆 Boss Challenge

Built a **Station Distance Analyzer**.

Tasks completed:

- Stored station coordinates as tuples
- Calculated distances using `math.sqrt()`
- Printed all station information
- Found the **farthest station**
- Printed a final distance report

---

🚀 **Python Journey — Day 23/100**
