Day 27 - JSON & GeoJSON Basics

📚 What I Learned

Today I learned:

- "json.dumps()"
- "json.loads()"
- "json.dump()"
- "json.load()"
- Writing JSON data to a file
- Reading JSON data from a file
- Understanding nested JSON structures
- Processing simple GeoJSON FeatureCollection data

I also practiced working with GIS-style station data stored in JSON and GeoJSON files.

---

💻 Practice

✔ Convert Python dictionaries to JSON

✔ Save JSON data to a file

✔ Load JSON data from a file

✔ Read specific values from JSON objects

✔ Work with nested dictionaries and lists

---

🌍 Mini GIS

Built a Station JSON Reader that:

- Saves weather station data to "stations.json"
- Loads the JSON file again
- Prints station names and coordinates
- Finds the station with the highest temperature

---

🏆 Boss Challenge

Built a GeoJSON Feature Analyzer that:

- Creates a simple FeatureCollection
- Saves it as "map.geojson"
- Loads and analyzes GeoJSON features
- Ignores stations with missing temperature values ("None")
- Prints valid stations with coordinates
- Detects the hottest station automatically

---

🚀 Python Journey — Day 28/100
