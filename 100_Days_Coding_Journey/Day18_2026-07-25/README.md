# Day 18 - index() and count()

## 📚 What I Learned

Today I learned:

- index()
- count()
- Using `in` before `index()` to avoid errors.
- Applying `index()` and `count()` with Python lists.

## 💻 Practice

✔ Find the position of an item using `index()`.

✔ Count how many times an item appears using `count()`.

✔ Check whether an item exists before using `index()`.

## 🌍 Mini GIS

Worked with a list of provinces by:

- Counting the total number of provinces.
- Checking if "Ha Noi" is in the list.
- Counting how many times "Hung Yen" appears.
- Finding the position of "Nam Dinh".

## 🏆 Boss Challenge

Worked with river data stored as a list of dictionaries.

Tasks completed:

- Printed river names and lengths.
- Created a list of river lengths.
- Found the longest river.
- Found the shortest river.
- Calculated the average river length.

## 🔥 Bonus Challenge

Worked without using `count()`.

Tasks completed:

- Used a `for` loop and `if` statement to count how many times "Song Hong" appears.

🚀 Python Journey — Day 18/100
